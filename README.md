# Academy Schedule Analyzer

Student Name: Basem Elnaba  
Cohort: `<add-your-cohort>`  
Assignment: 4

## Project

The console application is in [`AcademyScheduleAnalyzer/`](AcademyScheduleAnalyzer/). It manages and analyzes the supplied training schedule using arrays and functions, without LINQ or application model classes.

## Run the Console Application

```bash
dotnet restore
dotnet run --project AcademyScheduleAnalyzer
```

Run the BenchmarkDotNet comparison in Release mode:

```bash
dotnet run -c Release --project AcademyScheduleAnalyzer -- --benchmark
```

## LeetCode

See [`LeetCode/README.md`](LeetCode/README.md). Add your profile URL and your own accepted-submission screenshots before final submission.

## LinkedIn

See [`LinkedIn/README.md`](LinkedIn/README.md). Add the four published post URLs before final submission.

## Benchmark

See [`BENCHMARK.md`](BENCHMARK.md). Run the benchmark on your own machine and paste its result table before final submission.
